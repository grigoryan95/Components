import React from 'react';
import {Box, CircularProgress} from "@mui/material";

const Loader = () => {
    return (
        <div className="LoaderGif">
                <CircularProgress className='a'/>
        </div>
    );
};

export default Loader;